import { InputBase } from './input-base';

describe('InputBase', () => {
  it('should create an instance', () => {
    const directive = new InputBase();
    expect(directive).toBeTruthy();
  });
});
