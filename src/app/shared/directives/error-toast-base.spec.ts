import { ErrorToastBase } from './error-toast-base';

describe('ErrorToastBase', () => {
  it('should create an instance', () => {
    const directive = new ErrorToastBase();
    expect(directive).toBeTruthy();
  });
});
