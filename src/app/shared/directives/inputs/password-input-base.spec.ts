import { PasswordInputBase } from './password-input-base';

describe('PasswordInputBase', () => {
  it('should create an instance', () => {
    const directive = new PasswordInputBase();
    expect(directive).toBeTruthy();
  });
});
