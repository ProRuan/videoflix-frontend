import { EmailInputBase } from './email-input-base';

describe('EmailInputBase', () => {
  it('should create an instance', () => {
    const directive = new EmailInputBase();
    expect(directive).toBeTruthy();
  });
});
