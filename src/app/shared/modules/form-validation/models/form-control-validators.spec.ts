import { FormControlValidators } from './form-control-validators';

describe('FormControlValidators', () => {
  it('should create an instance', () => {
    expect(new FormControlValidators()).toBeTruthy();
  });
});
