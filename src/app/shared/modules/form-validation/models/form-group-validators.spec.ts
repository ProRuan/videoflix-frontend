import { FormGroupValidators } from './form-group-validators';

describe('FormGroupValidators', () => {
  it('should create an instance', () => {
    expect(new FormGroupValidators()).toBeTruthy();
  });
});
