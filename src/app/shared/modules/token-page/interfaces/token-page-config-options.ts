import { TokenPageConfig } from './token-page-config';

/**
 * Interface representing options for a token page configuration.
 */
export interface TokenPageConfigOptions {
  error: TokenPageConfig;
  success: TokenPageConfig;
}
