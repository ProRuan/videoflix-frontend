import { OverlayManagerBase } from './overlay-manager-base';

describe('OverlayManagerBase', () => {
  it('should create an instance', () => {
    expect(new OverlayManagerBase()).toBeTruthy();
  });
});
