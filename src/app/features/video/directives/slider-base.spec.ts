import { SliderBase } from './slider-base';

describe('SliderBase', () => {
  it('should create an instance', () => {
    const directive = new SliderBase();
    expect(directive).toBeTruthy();
  });
});
