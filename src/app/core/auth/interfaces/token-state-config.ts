/**
 * Interface representing a token state configuration.
 */
export interface TokenStateConfig {
  title: string;
  color: string;
  messages: string[];
  primText: string;
  primRoute: string;
  secText?: string;
  secRoute?: string;
}
