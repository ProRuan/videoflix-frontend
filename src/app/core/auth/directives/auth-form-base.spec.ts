import { AuthFormBase } from './auth-form-base';

describe('AuthFormBase', () => {
  it('should create an instance', () => {
    const directive = new AuthFormBase();
    expect(directive).toBeTruthy();
  });
});
